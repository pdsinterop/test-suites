{
  "name": "ocm-test-suite",
  "version": "1.0.0",
  "description": "Test Suite that test interaction between servers that are federated through Open Cloud Mesh (OCM)",
  "main": "index.js",
  "scripts": {
    "test": "HEADLESS=1 jest *.test.js --runInBand",
    "debug": "jest *.test.js --runInBand"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "jest": "^26.6.3",
    "puppeteer": "^5.5.0",
    "solid-auth-fetcher": "^1.3.5"
  }
}
